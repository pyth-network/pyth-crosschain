global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.sample.yml

scrape_configs:
  - job_name: "okx_recorder"
    scrape_interval: 15s
    static_configs:
      - targets: ["okx-recorder:9095"]
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
        replacement: "okx_recorder"
