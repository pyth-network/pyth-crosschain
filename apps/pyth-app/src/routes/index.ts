const billing = () => "/billing";
const dashboard = () => "/";
const feeds = () => "/feeds";
const integration = () => "/integration";
const home = dashboard;

export const uiRoutes = {
  billing,
  dashboard,
  home,
  feeds,
  integration,
};
