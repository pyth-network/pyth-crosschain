[package]
name = "pyth"
version = "0.1.0"

[dependencies]
# TODO: pin dependencies
# pin to 
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework", rev = "61d920a" }

[addresses]
# TODO: correct these
pyth = "0x0"
