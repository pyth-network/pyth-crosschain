{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["//#install:modules", "^build"],
      "inputs": ["src/**/*.ts"],
      "outputs": ["dist/**"]
    }
  }
}

