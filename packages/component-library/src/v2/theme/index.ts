// we intentionally do not expose the internally-used
// style-funcs.ts, as those are only for consumption by the
// component library itself.
// however, we definitely want the new theme exposed
export * from "./theme";
