{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build:storybook": {
      "outputs": ["storybook-static/**"]
    },
    "start:dev": {
      "persistent": true,
      "cache": false
    }
  }
}
