/**
 * static no-op function. useful
 * for when you just need a function
 * as a placeholder
 */
export const NOOP = () => {
  /* nothing */
};

/**
 * static no-op function that returns null.
 * useful for when you just need a function as a placeholder
 */
// eslint-disable-next-line unicorn/no-null
export const NOOP_NULL = () => null;
