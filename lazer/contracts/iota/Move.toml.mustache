# Generated by `manage_iota_lazer_contract.ts`, only edit `.mustache` file.
# Make sure to commit changes to `Move.*.toml` files.

[package]
name = "pyth_lazer"
edition = "2024"

[dependencies]
wormhole = { local = "./vendor/wormhole_{{chain_name}}" }

[addresses]
pyth_lazer = "{{lazer_address}}"
